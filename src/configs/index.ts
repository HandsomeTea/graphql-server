import { trace, log, audit, system, updateOrCreateLogInstance, traceId } from './logger';
import getENV from './envConfig';

export {
    trace,
    log,
    audit,
    system,
    updateOrCreateLogInstance,
    getENV,
    traceId
};
